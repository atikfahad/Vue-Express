Vue-Cli : npm install -g vue-cli
// vue list -> Available Options

//GIT
  git clone something.git
  git status -> status of the git
    git add -all
    git commit -m 'init the vuejs application using vue-cli'
    git push -> it will push to main-repo


// NPM
  npm init -f -> it will create all default .package file
  node ./node_modules/eslint/bin/eslint.js --init // We have initialize


  // In Server file
    "start": "./node_modules/nodemon/bin/nodemon.js src/app.js --exec 'npm run lint && node'",
    "lint": "./node_modules/.bin/eslint **/*.js "  

// CORS (module) :-> onno server theke request ashar jonno
// MORGAN (module) :-> request k log akare dekhai terminal e